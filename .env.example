# Required: power the agent
ANTHROPIC_API_KEY=sk-ant-...

# Optional: live fixtures from football-data.org (free tier works).
# Leave blank to use bundled mock data — the demo still runs end to end.
FOOTBALL_DATA_API_KEY=

# Optional overrides
PITCHSIDE_MODEL=claude-sonnet-4-6
